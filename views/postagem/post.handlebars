<div class="jumbotron">
    <div class="container">
        <h1 class="display-3">{{postagem.titulo}}</h1>
        {{!-- <p class="lead">{{postagem.data}}</p> --}}
        <hr class="my-2">
        <p>{{postagem.categoria.nome}}</p>
    </div>
</div>

<div class="container">
    <div class="card">
        <div class="card-body">
            <p class="card-text">{{postagem.descricao}}</p>
            <p class="card-text">{{postagem.conteudo}}</p>
        </div>
    </div>
</div>